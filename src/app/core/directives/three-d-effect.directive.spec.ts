import { ThreeDEffectDirective } from './three-d-effect.directive';

describe('ThreeDEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeDEffectDirective();
    expect(directive).toBeTruthy();
  });
});
