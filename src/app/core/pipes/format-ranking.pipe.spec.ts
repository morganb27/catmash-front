import { FormatRankingPipe } from './format-ranking.pipe';

describe('FormatRankingPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRankingPipe();
    expect(pipe).toBeTruthy();
  });
});
